import AppShell from '@/components/AppShell'

// AppLayout delegates to AppShell — the full-featured layout with sidebar,
// topbar, breadcrumb, notification bell, and mobile Sheet drawer.
export default function AppLayout() {
  return <AppShell />
}
